# Google SERP Scraping Tool Environment Configuration
# Copy this file to .env and fill in your values

# Primary provider: Google Custom Search JSON API (Recommended)
# Get API key from Google Cloud Console and CX from Programmable Search Engine
GOOGLE_API_KEY=# Primary API key from Google Cloud Console (Custom Search JSON API)
GOOGLE_CSE_CX=# Primary Search Engine ID (CX) from Programmable Search Engine

GOOGLE_API_KEY_BACKUP=# Backup API key (optional) - used if primary key hits quota
GOOGLE_CX_BACKUP=# Backup Search Engine ID (optional) - paired with backup API key

# Debug mode (Optional)
# Set to true for verbose logging and debug information
DEBUG=false

# Log level (Optional)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Whether to apply locale hints to Google CSE (Optional)
# When true and language/country are set, lr=lang_xx and gl=cc will be used
APPLY_LOCALE_HINTS=false

# Maximum concurrent scraping jobs (Optional)
# Number of keywords that can be scraped simultaneously
MAX_CONCURRENT_JOBS=5

# Request timeout in seconds (Optional)
# How long to wait for API responses
REQUEST_TIMEOUT=300

# Cache settings (Optional)
# Cache expiration time in seconds (default 86400 = 24h)
CACHE_TTL_SECONDS=86400
